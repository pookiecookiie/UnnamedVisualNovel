extends Node




func _ready() -> void:
	pass



func get_character_sprite(character_name: String, emotion: String = "neutral") -> void:
	#assert(character_name in characters)
	##assert(emotion in characters[character_name].emotions)
	#return characters[character_name].emotions[emotion]
	pass


func get_background(background_name: String):
	#assert(background_name in backgrounds)
	#return backgrounds[background_name]
	pass

